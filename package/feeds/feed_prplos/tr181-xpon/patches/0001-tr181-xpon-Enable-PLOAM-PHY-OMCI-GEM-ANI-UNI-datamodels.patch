From b502d8446ae71c4ec0596f3caeecd5393bc18393 Mon Sep 17 00:00:00 2001
From: Harini Pavithra Elangovan <helangov@qti.qualcomm.com>
Date: Thu, 9 Apr 2026 10:40:10 +0530
Subject: [PATCH 1/1] tr181 xpon Enable PLOAM PHY OMCI GEM ANI UNI datamodels

Signed-off-by: Harini Pavithra Elangovan <helangov@qti.qualcomm.com>
---
 include_priv/dm_info.h            | 10 ++--
 odl/tr181-xpon_ANI.odl.m4         | 48 ++++++++++++++++++++-
 odl/tr181-xpon_EthernetUNI.odl.m4 | 49 ++++++++++++++++++++-
 src/dm_actions.c                  |  8 ++--
 src/dm_info.c                     | 79 +++++++++++++++++++++++--------
 5 files changed, 165 insertions(+), 29 deletions(-)

diff --git a/include_priv/dm_info.h b/include_priv/dm_info.h
index 4ea3925..1b15c9f 100644
--- a/include_priv/dm_info.h
+++ b/include_priv/dm_info.h
@@ -87,10 +87,12 @@ typedef enum _object_id {
     oid_ani_tc_authentication,
     oid_ani_tc_performance_thresholds,
     oid_ani_tc_alarms,
-    oid_ani_tc_pm_omci,
-    oid_ani_tc_pm_phy,
-    oid_ani_tc_pm_gem,
-    oid_ani_tc_pm_ploam,
+    obj_id_ani_stats,
+    obj_id_eth_stats,
+    obj_id_tc_pm_omci,
+    obj_id_tc_pm_phy,
+    obj_id_tc_pm_gem,
+    obj_id_tc_pm_ploam,
     oid_ani_tc_gem_port_pm,
     oid_nbr,
     oid_unknown = oid_nbr
diff --git a/odl/tr181-xpon_ANI.odl.m4 b/odl/tr181-xpon_ANI.odl.m4
index f33c958..a1b2c3d 100644
--- a/odl/tr181-xpon_ANI.odl.m4
+++ b/odl/tr181-xpon_ANI.odl.m4
@@ -176,7 +176,53 @@
             on action validate call check_enum
                 ["Unknown", "G-PON", "XG-PON", "NG-PON2", "XGS-PON" ];
         }
-
+        object Stats {
+            %read-only %volatile uint32 BytesSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BytesReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 PacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 PacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 ErrorsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 ErrorsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnicastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnicastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 DiscardPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 DiscardPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 MulticastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 MulticastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BroadcastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BroadcastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnknownProtoPacketsReceived {
+                on action read call read_param;
+            }
+        }
         /**
          * Small-form Factor Pluggable (SFP) entries associated with this
          * interface.
diff --git a/odl/tr181-xpon_EthernetUNI.odl.m4 b/odl/tr181-xpon_EthernetUNI.odl.m4
index 8511319..b4c5d6e 100644
--- a/odl/tr181-xpon_EthernetUNI.odl.m4
+++ b/odl/tr181-xpon_EthernetUNI.odl.m4
@@ -231,7 +231,54 @@
         %read-only string InterdomainName {
             on action validate call check_maximum_length 25;
         }
-
+        object Stats {
+            %read-only %volatile uint32 BytesSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BytesReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 PacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 PacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 ErrorsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 ErrorsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnicastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnicastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 DiscardPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 DiscardPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 MulticastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 MulticastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BroadcastPacketsSent {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 BroadcastPacketsReceived {
+                on action read call read_param;
+            }
+            %read-only %volatile uint32 UnknownProtoPacketsReceived {
+                on action read call read_param;
+            }
+        }
+	
         ifdef(`CONFIG_SAH_AMX_TR181_XPON_USE_NETDEV_COUNTERS', /**
         * Throughput statistics for this interface.
         *
diff --git a/src/dm_actions.c b/src/dm_actions.c
index 1ac11bc..19d07ee 100644
--- a/src/dm_actions.c
+++ b/src/dm_actions.c
@@ -199,10 +199,10 @@ amxd_status_t _read_param(amxd_object_t* object,
 
 #if !defined(SUPPORT_PM_OBJECTS)
     /* Do not query the parameters of the PM objects */
-    if((oid_ani_tc_pm_omci == oid) ||
-       (oid_ani_tc_pm_phy == oid) ||
-       (oid_ani_tc_pm_gem == oid) ||
-       (oid_ani_tc_pm_ploam == oid) ||
+    if((obj_id_tc_pm_omci == oid) ||
+       (obj_id_tc_pm_phy == oid) ||
+       (obj_id_tc_pm_gem == oid) ||
+       (obj_id_tc_pm_ploam == oid) ||
        (oid_ani_tc_gem_port_pm == oid)) {
         SAH_TRACEZ_DEBUG(ME, "%s: this PM object is not supported", path);
         rv = 0; /* success */
diff --git a/src/dm_info.c b/src/dm_info.c
index 3e7cf14..f3b5507 100644
--- a/src/dm_info.c
+++ b/src/dm_info.c
@@ -147,13 +147,13 @@ static const param_info_t PERFORMANCE_THRESHOLDS_PARAMS[] = {
     { .name = "SignalDegrade", .type = AMXC_VAR_ID_UINT32 }
 };
 
-static const param_info_t PM_OMCI_PARAMS[] = {
+static const param_info_t TC_PM_OMCI[] = {
     { .name = "BaselineMessagesReceived", .type = AMXC_VAR_ID_UINT64 },
     { .name = "ExtendedMessagesReceived", .type = AMXC_VAR_ID_UINT64 },
     { .name = "MICErrors", .type = AMXC_VAR_ID_UINT32 }
 };
 
-static const param_info_t PM_PHY_PARAMS[] = {
+static const param_info_t TC_PM_PHY[] = {
     { .name = "CorrectedFECBytes", .type = AMXC_VAR_ID_UINT64 },
     { .name = "CorrectedFECCodewords", .type = AMXC_VAR_ID_UINT64 },
     { .name = "UncorrectableFECCodewords", .type = AMXC_VAR_ID_UINT64 },
@@ -163,14 +163,14 @@ static const param_info_t PM_PHY_PARAMS[] = {
     { .name = "UnknownProfile", .type = AMXC_VAR_ID_UINT32 }
 };
 
-static const param_info_t PM_GEM_PARAMS[] = {
+static const param_info_t TC_PM_GEM[] = {
     { .name = "FramesSent", .type = AMXC_VAR_ID_UINT64 },
     { .name = "FramesReceived", .type = AMXC_VAR_ID_UINT64 },
     { .name = "FrameHeaderHECErrors", .type = AMXC_VAR_ID_UINT32 },
     { .name = "KeyErrors", .type = AMXC_VAR_ID_UINT32 }
 };
 
-static const param_info_t PM_PLOAM_PARAMS[] = {
+static const param_info_t TC_PM_PLOAM[] = {
     { .name = "MICErrors", .type = AMXC_VAR_ID_UINT32 },
     { .name = "DownstreamMessageCount", .type = AMXC_VAR_ID_UINT64 },
     { .name = "RangingTime", .type = AMXC_VAR_ID_UINT64 },
@@ -200,6 +200,25 @@ static const param_info_t TC_ALARMS_PARAMS[] = {
     { .name = "ROGUE", .type = AMXC_VAR_ID_BOOL }
 };
 
+static const param_info_t STATS_PARAMS[] = {
+    { .name = "BytesSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "BytesReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "PacketsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "PacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "ErrorsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "ErrorsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "UnicastPacketsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "UnicastPacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "DiscardPacketsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "DiscardPacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "MulticastPacketsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "MulticastPacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "BroadcastPacketsSent", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "BroadcastPacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+    { .name = "UnknownProtoPacketsReceived", .type = AMXC_VAR_ID_UINT32 },
+};
+
+
 /**
  * Array with info about objects in the XPON DM.
  *
@@ -234,7 +253,7 @@ const object_info_t OBJECT_INFO[oid_nbr] = {
         .name = "EthernetUNI",
         .generic_path = "XPON.ONU.x.EthernetUNI",
         .key_name = NAME_PARAM,
-        .singletons = NULL,
+        .singletons = "Stats",
         .templates = NULL,
         .params = ETHERNET_UNI_PARAMS,
         .n_params = ARRAY_SIZE(ETHERNET_UNI_PARAMS),
@@ -245,7 +264,7 @@ const object_info_t OBJECT_INFO[oid_nbr] = {
         .name = "ANI",
         .generic_path = "XPON.ONU.x.ANI",
         .key_name = NAME_PARAM,
-        .singletons = "TC.ONUActivation,TC.PerformanceThresholds,TC.Alarms",
+        .singletons = "TC.ONUActivation,TC.PerformanceThresholds,TC.Alarms,Stats,TC.PM.PLOAM,TC.PM.PHY,TC.PM.OMCI,TC.PM.GEM",
         .templates = "TC.GEM.Port,Transceiver",
         .params = ANI_PARAMS,
         .n_params = ARRAY_SIZE(ANI_PARAMS),
@@ -257,7 +276,7 @@ const object_info_t OBJECT_INFO[oid_nbr] = {
         .generic_path = "XPON.ONU.x.ANI.x.TC.GEM.Port",
         .key_name = "PortID",
         .key_max_value = 65534,
-        .singletons = NULL,
+        .singletons = "PM",
         .templates = NULL,
         .params = GEM_PORT_PARAMS,
         .n_params = ARRAY_SIZE(GEM_PORT_PARAMS),
@@ -320,47 +339,69 @@ const object_info_t OBJECT_INFO[oid_nbr] = {
         .has_rw_enable = false
     },
     {
-        .id = oid_ani_tc_pm_omci,
+        .id = obj_id_ani_stats,
+        .name = "Stats",
+        .generic_path = "XPON.ONU.x.ANI.x.Stats",
+        .key_name = NULL,
+        .singletons = NULL,
+        .templates = NULL,
+        .params = STATS_PARAMS,
+        .n_params = ARRAY_SIZE(STATS_PARAMS),
+        .has_rw_enable = false
+    },
+    {
+        .id = obj_id_eth_stats,
+        .name = "Stats",
+        .generic_path = "XPON.ONU.x.EthernetUNI.x.Stats",
+        .key_name = NULL,
+        .singletons = NULL,
+        .templates = NULL,
+        .params = STATS_PARAMS,
+        .n_params = ARRAY_SIZE(STATS_PARAMS),
+        .has_rw_enable = false
+    },
+    {
+        .id = obj_id_tc_pm_omci,
         .name = "TC.PM.OMCI",
         .generic_path = "XPON.ONU.x.ANI.x.TC.PM.OMCI",
         .key_name = NULL,
         .singletons = NULL,
         .templates = NULL,
-        .params = PM_OMCI_PARAMS,
-        .n_params = ARRAY_SIZE(PM_OMCI_PARAMS),
+        .params = TC_PM_OMCI,
+        .n_params = ARRAY_SIZE(TC_PM_OMCI),
         .has_rw_enable = false
     },
     {
-        .id = oid_ani_tc_pm_phy,
+        .id = obj_id_tc_pm_phy,
         .name = "TC.PM.PHY",
         .generic_path = "XPON.ONU.x.ANI.x.TC.PM.PHY",
         .key_name = NULL,
         .singletons = NULL,
         .templates = NULL,
-        .params = PM_PHY_PARAMS,
-        .n_params = ARRAY_SIZE(PM_PHY_PARAMS),
+        .params = TC_PM_PHY,
+        .n_params = ARRAY_SIZE(TC_PM_PHY),
         .has_rw_enable = false
     },
     {
-        .id = oid_ani_tc_pm_gem,
+        .id = obj_id_tc_pm_gem,
         .name = "TC.PM.GEM",
         .generic_path = "XPON.ONU.x.ANI.x.TC.PM.GEM",
         .key_name = NULL,
         .singletons = NULL,
         .templates = NULL,
-        .params = PM_GEM_PARAMS,
-        .n_params = ARRAY_SIZE(PM_GEM_PARAMS),
+        .params = TC_PM_GEM,
+        .n_params = ARRAY_SIZE(TC_PM_GEM),
         .has_rw_enable = false
     },
     {
-        .id = oid_ani_tc_pm_ploam,
+        .id = obj_id_tc_pm_ploam,
         .name = "TC.PM.PLOAM",
         .generic_path = "XPON.ONU.x.ANI.x.TC.PM.PLOAM",
         .key_name = NULL,
         .singletons = NULL,
         .templates = NULL,
-        .params = PM_PLOAM_PARAMS,
-        .n_params = ARRAY_SIZE(PM_PLOAM_PARAMS),
+        .params = TC_PM_PLOAM,
+        .n_params = ARRAY_SIZE(TC_PM_PLOAM),
         .has_rw_enable = false
     },
     {
-- 
2.34.1
